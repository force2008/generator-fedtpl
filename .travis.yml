language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fedtpl' ]; then cd .. && eval "mv $currentfolder generator-fedtpl" && cd generator-fedtpl; fi

